# API do Servidor ESP32 — PASS-BY Catraca de Animais

API REST:
 - servida pelo próprio ESP32 (mesma origem da página HTML),  
 - consumida pelo cliente Web em `index.html` / `js/script.js`. - Formato de dados: JSON. Sem autenticação —> destinada a uso em rede local

 